/**
 * cookie interface which all different types of cookie implement (single-sided,
 * double-sided)
 * 
 * @author OOP Gruppe 187
 * 
 */
public interface Keks {
	// could contains methods common to all cookies like calculateCalories()
}
